# 2026-06-08 Project Log

## 本轮目标

Review the first six generated single-slide tax-comparison images and identify why they failed the user's taste bar.

## 用户反馈

- All six generated images were rejected.
- The outputs were too simple and looked like ordinary card-based charts.
- If the result is only a plain chart layout, HTML or normal PPT editing is faster.
- The value of image generation should be the ability to create visuals beyond ordinary HTML/PPT layouts.
- Future defaults should target 4K when the generation path supports explicit size control.

## 关键发现

- The first prompt set over-constrained the model into plain "two panels plus donut charts" output.
- The templates were treated mostly as color palettes instead of complete art-direction systems.
- Negative prompts such as "no decorative clutter" removed too much visual richness.
- The prompts lacked role framing, premium visual ambition, information hierarchy, material depth, background graphics, and explicit color usage rules.
- Dense Chinese tax labels remain a text-accuracy risk even when the visual layout is acceptable.

## 分辨率记录

- The six built-in `image_gen` outputs were `1672x941`, not 2K or 4K.
- Local imagegen fallback documentation says `gpt-image-2` CLI/API workflows can request `3840x2160` for 4K landscape output.
- Built-in `image_gen` does not expose the same explicit size controls in the current Codex tool surface.

## 下一轮方向

- Rewrite prompts as high-end one-page visual design briefs, not chart layout instructions.
- Add "visual ambition" to the acceptance rubric.
- Use 4K CLI/API fallback for final-quality tests when `OPENAI_API_KEY` and network access are available.
- Consider a hybrid workflow when exact Chinese text matters: AI generates premium visual assets/background, deterministic HTML/PPT rendering places exact charts and text, final output is flattened as a 4K image.

## A/B Test: Pure AI vs Hybrid

The user asked to compare two approaches instead of assuming the hybrid workflow is better.

Approach A:

- Pure built-in image generation, one prompt, whole-slide image with text and charts.
- Output copied locally as `outputs/ab-tax-2026-06-08/pure-ai-tax-comparison.png`.
- Actual dimensions: `1672x941`.
- Strength: stronger visual atmosphere, premium navy/gold tone, symbolic imagery, more obvious image-generation value.
- Weakness: text is not fully controllable; the model changed wording and inserted extra symbolic elements.

Approach B:

- Deterministic HTML/CSS composition exported by Chrome headless.
- Output saved locally as `outputs/ab-tax-2026-06-08/hybrid-tax-comparison.png`.
- Actual dimensions: `3840x2160`.
- Strength: exact Chinese text, exact numbers, deterministic charts, true 4K output.
- Weakness: still reads more like a high-end HTML/PPT infographic than an image-model-native visual.

Interim conclusion:

- Pure AI has a higher visual ceiling but lower text/data reliability.
- Hybrid has higher production reliability but needs richer art direction or AI-generated background/assets to avoid looking like polished HTML.
- The next useful test is not "plain HTML vs pure AI"; it is "pure AI final page" vs "AI-generated premium visual system + deterministic text/chart overlay".

## Follow-up A/B: AI Background + Exact Overlay

The user approved testing the next version: pure AI final page versus AI-generated premium visual system/background/assets plus deterministic HTML overlay.

Approach C:

- Generate an AI background/visual-system asset with no text and no charts.
- Overlay exact Chinese text and deterministic charts in HTML/CSS.
- Export the final composition through Chrome headless at `3840x2160`.
- Local files:
  - `outputs/ab-tax-2026-06-08/ai-visual-system-background.png`
  - `outputs/ab-tax-2026-06-08/ai-bg-overlay-tax-comparison.png`

Findings:

- Prompting the built-in `image_gen` tool with "4K-quality" did not change the actual output dimensions; the background asset was still `1672x941`.
- The final overlay export is true `3840x2160` because the HTML renderer controls the canvas size.
- Approach C keeps some AI visual atmosphere from Approach A while preserving exact text and numbers like Approach B.
- User feedback after reviewing Approach C: the effect is too weak. The correct project path is pure AI whole-slide finished images, not AI background/visual system plus HTML precise overlay.

Final route decision:

- Pure AI finished-page generation is the V1 main path.
- Hybrid overlay is no longer a default or recommended path for this project stage.
- The prompt system should improve pure AI visual ambition, composition, material depth, and page-level art direction instead of trying to recover quality with deterministic overlay.
- Hybrid overlay may remain as a fallback when exact text/data is more important than visual ceiling, but it should not drive the core skill design.

Resolution rule:

- Prompt words like "4K" can improve perceived sharpness or style intent, but cannot guarantee actual pixel dimensions when the tool surface does not expose a size parameter.
- Actual 4K generation requires a size-controlled path such as CLI/API `gpt-image-2 --size 3840x2160`, or a deterministic renderer such as HTML/PPT export at `3840x2160`.

## Gemini Nano Banana Pro Attempt

The user provided a Gemini API key locally under Downloads and asked to start a pure AI finished-page generation test.

Test setup:

- Model endpoint checked successfully before generation: `models/gemini-3-pro-image-preview`, display name `Nano Banana Pro`.
- Requested generation path: pure AI whole-slide finished image.
- Requested size controls: `aspectRatio=16:9`, `imageSize=4K`.
- Test prompt: `market-slate` style, Chinese/US 2024 tax revenue composition comparison.

Result:

- Generation request returned `429 RESOURCE_EXHAUSTED`.
- Error reason: quota exceeded for `gemini-3-pro-image`; the free-tier request and input-token limits for this model are currently `0` on the key's Google project.
- No image was generated.

Implication:

- The API key is valid and can see the model, but the Google project needs usable paid quota or a key from a project with Nano Banana Pro image-generation quota before this route can be tested.
- Do not retry this exact request repeatedly until quota/billing is fixed; it will not improve prompt quality or image output.

## Gemini Nano Banana Pro Pricing Check

The user asked for the API price of `gemini-3-pro-image-preview / Nano Banana Pro` with `aspectRatio=16:9` and `imageSize=4K`.

Official Google pricing currently maps Nano Banana Pro to Gemini 3 Pro Image pricing:

- Free tier: not available for Gemini 3 Pro Image.
- Standard paid tier image output: `$120.00 / 1,000,000 image output tokens`.
- 1K/2K output image: `$0.134` per image.
- 4K output image: `$0.24` per image.
- Batch or Flex 4K output image: `$0.12` per image.
- Input text/image cost is separate, but for normal prompt-only slide generation the dominant cost is the output image.

Cost implication for TasteCraft testing:

- 6-template single-slide horizontal review at 4K Standard: about `$1.44` plus small input-token cost.
- 6 templates with up to 3 attempts each: about `$4.32` plus small input-token cost.
- If Batch/Flex is acceptable, image output cost is roughly half, but latency and availability tradeoffs need separate testing.

User routing decision:

- Use Standard for normal interactive testing.
- Use Batch for larger batch generation runs.
- Do not use Priority for TasteCraft testing.
- Flex is not the default unless the user explicitly wants lower cost while preserving synchronous-style calls.

OAuth clarification:

- Google AI Pro / Google One provides expanded usage for Google consumer and product surfaces such as Gemini, AI Studio, Flow, and related Google apps.
- The Gemini API can authenticate with OAuth, but OAuth is an authentication method tied to a Google Cloud project and enabled API. It does not automatically convert Google AI Pro consumer subscription limits into free Gemini API image-generation quota.
- For automated generation from Codex, the reliable route remains Gemini API with a Google Cloud project that has usable paid Gemini API quota, using Standard for normal work and Batch for bulk runs.

## Google AI Pro Subscription Capability Check

The user showed a Google One subscription screenshot: `Google AI Pro (5 TB)`.

Official plan interpretation:

- Google AI Plus: 200 GB storage, 2x Gemini usage limits, more access to Pro models, limited Nano Banana Pro image generation/editing, 200 Google Flow credits/month.
- Google AI Pro: 5 TB storage, 4x Gemini usage limits, expanded Pro-model access, expanded Nano Banana Pro image generation/editing, 1M context window, 1,000 Google Flow credits/month, Gemini in Gmail/Docs/Vids and related Google products.
- Google AI Ultra 5x: 20 TB storage, 5x AI Pro usage limits, higher AI Studio / Gemini / NotebookLM / Flow limits, 10,000 Flow credits/month.
- Google AI Ultra 20x: 30 TB storage, 20x AI Pro usage limits, highest listed limits, 25,000 Flow credits/month.

Decision:

- The user's `Google AI Pro (5 TB)` plan should support Nano Banana Pro in Gemini app / Google AI Studio / Flow-style Google product surfaces, subject to country, age, app availability, and changing usage limits.
- It does not by itself unlock Codex-side Gemini API image generation. Codex automation still needs a Gemini API key or OAuth credential backed by a Google Cloud project with usable Gemini API quota/billing.

## CC Switch And Gemini CLI Recheck

The user challenged the previous conclusion and mentioned `ccswitch`.

Updated finding:

- The user's correction is partly right. Google official Gemini CLI documentation says local Gemini CLI supports `Login with Google`, and explicitly says this method should be used by Google AI Pro or Google AI Ultra subscribers.
- Gemini CLI quota/pricing documentation also says Google AI Pro and AI Ultra can be used as a fixed-price paid tier for Gemini CLI / Gemini Code Assist-style usage after signing up through the Gemini Code Assist setup flow.
- `CC Switch` is a real third-party/open-source desktop manager for Claude Code, Codex, Gemini CLI, OpenCode, OpenClaw, and related provider configurations. It can manage provider presets, Codex configs, Gemini CLI configs, local proxying, MCP, prompts, skills, and switching.

Important boundary:

- This confirms a non-AI-Studio path exists for Gemini CLI with Google login.
- This does not yet prove Codex can directly call Nano Banana Pro 4K image generation through the Google AI Pro consumer subscription.
- The officially documented CLI path is primarily a Gemini CLI / Gemini Code Assist workflow. The docs say model requests are made across the Gemini model family as determined by Gemini CLI, but they do not document `gemini-3-pro-image-preview`, `imageSize=4K`, or a stable image file output contract for TasteCraft's PPT image workflow.
- Therefore the truth status is: plausible and testable, not confirmed for production use until local Gemini CLI OAuth plus image generation is tested end to end.

Next validation path:

- Install or run Gemini CLI locally.
- Authenticate with the user's Google AI Pro account through `Login with Google`.
- Check available models/capabilities from the CLI.
- Try one pure AI slide-image prompt and inspect whether the CLI can return/save an actual image file.
- If successful, optionally use CC Switch only as a configuration manager or bridge; do not treat CC Switch as the source of quota or model access.

## Gemini CLI Local Install

The user asked to skip CC Switch for now and test direct Codex-to-Gemini-CLI usage.

Install result:

- Official package installed: `@google/gemini-cli`.
- Install location: `/Users/rongxing/.local/bin/gemini`.
- Installed version: `0.45.2`.
- System-level install to `/usr/local` failed with macOS `EACCES`, so the CLI was installed under the user's local prefix instead.
- Running the CLI without elevated filesystem permissions initially failed to create `/Users/rongxing/.gemini`; running with the proper permission context initialized successfully.

Next step:

- Run the CLI interactively and choose `Login with Google`.
- Complete browser OAuth with the user's Google AI Pro account.
- After login, test whether the CLI can call an image-capable route and save a Nano Banana Pro output image.

## Gemini CLI OAuth Run

The user asked Codex to run Gemini CLI directly.

Run result:

- Gemini CLI launched from `/Users/rongxing/.local/bin/gemini --skip-trust`.
- Authentication method selected: `Sign in with Google`.
- Browser OAuth completed successfully.
- CLI reported: `Signed in with Google`.
- CLI reported plan: `Gemini Code Assist in Google One AI Pro`.
- Local Gemini config files now exist under `~/.gemini/`, including OAuth credential files. File contents were not read or committed.

First request test:

- Prompt sent: "请用一句中文确认：Gemini CLI 已经可以通过 Google AI Pro 账号响应请求。"
- Result: Google returned an account security gate before the model response.
- CLI message: `Further action is required to use this service.`
- Required action shown by CLI: `Verify your account`.
- Verification flow waited for browser-side completion and then a terminal Enter confirmation.
- The session was cancelled after the verification did not complete during the run; no image-generation test was attempted yet.

Current status:

- The Google AI Pro subscription path through Gemini CLI is confirmed at the login/plan-recognition level.
- It is not yet confirmed at the model-response or image-generation level because Google account verification is still required.
- After the user completes Google account verification, rerun a small text request first, then test Nano Banana Pro image generation.

## Gemini CLI Verification Paused

The user reported that the Google account verification step cannot be completed because SMS verification is unavailable.

Decision:

- Pause the Gemini CLI / Google AI Pro subscription route for now.
- Keep the installed Gemini CLI and existing `~/.gemini` login files in place, but do not use them for TasteCraft generation tests until account verification can be completed.
- Treat the route status as: login and plan recognition confirmed; actual model response and Nano Banana Pro image generation not confirmed.

Practical implication:

- Current viable automated route remains Gemini API with a Google Cloud / AI Studio project that has usable paid image-generation quota.
- If the user later resolves Google account verification, resume with a small text request before attempting image generation.

## Six Template Rewrite For Codex Image Generation

The user asked to use Codex image generation first and rewrite the six prompt templates.

Implementation:

- Rewrote console prompt generation around pure AI whole-slide finished presentation images.
- Added six complete art-direction systems instead of treating templates as palette swaps:
  - `citrus-editorial`: Editorial Paper / non-literal editorial paper system
  - `market-slate`: executive market intelligence
  - `atelier-rose`: Sovereign Gold / sovereign private wealth
  - `harbor-mint`: Precision Blueprint / precision blueprint system
  - `ink-copper`: Ink Copper / mineral copper executive keynote
  - `orchard-lab`: Evidence Lab / evidence lab dossier
- Added explicit prompt language for senior business visual design, 4K target when supported, visual ambition, information architecture, color usage, typography, exact-text risk, and no separate HTML/PPT overlay.
- Updated negative prompts to block generic card layouts, default dashboards, flat HTML-like charts, invented numbers, and garbled text.
- Fixed palette switching so generated prompts use the selected preset's actual color values unless custom colors are enabled.

Validation:

- Added a Node-backed unit test that verifies all six templates generate pure-AI whole-slide prompts with template-specific art direction and matching palette colors.
- Targeted test passed: `python3 -m unittest tests.test_console_prompt_templates`.
- Full local validation passed: skill metadata, strict references, schema examples, console smoke, sensitive-file scan, dist dry-run, and unit tests.

Codex image preview observations:

- Generated one preview image per template with the built-in Codex image tool. Images were treated as preview artifacts and were not committed to the public repository.
- The exact prompts and image path mapping were backfilled into `docs/project-log/2026-06-08-codex-image-prompts.md`.
- `ink-copper` and `orchard-lab` produced the strongest visual ceiling in this pass.
- `market-slate` and `harbor-mint` remain at risk of drifting back toward ordinary dashboard/card layouts.
- `citrus-editorial` became more visually distinctive, but the metaphor can feel less natural for a tax-comparison board slide.
- Text accuracy still needs human review per protocol before any template is marked accepted.

## Bilingual Prompt Variants

The user clarified that prompt templates need both Chinese and English versions.

Implementation:

- Confirmed the previous Codex image-generation inputs were mixed prompts: English prompt structure and art direction, with Chinese slide title, labels, numbers, and some Chinese text rules.
- Added bilingual console prompt generation:
  - `prompt_variants.zh-CN`
  - `prompt_variants.en`
- Changed the default Chinese UI path so `positive_prompt`, `negative_prompt`, and `reference_notes` mirror the Chinese variant, while the English version stays available in `prompt_variants.en`.
- Extended `schema/prompt-pack.schema.json` to allow bilingual prompt variants and updated the sample prompt pack accordingly.

Validation:

- Targeted bilingual tests passed: `python3 -m unittest tests.test_console_prompt_templates`.
- Schema example validation and console smoke test passed after the schema/sample update.

## User Review And Template Repositioning

The user reviewed the six Codex-generated images and provided template-level feedback.

Decisions:

- `citrus-editorial` should not be a finance / serious-report default. Keep the useful editorial paper texture, but remove literal citrus, flowers, food, plants, lifestyle props, and still-life cues.
- `market-slate` stays as the safe professional choice. A light, content-relevant bottom summary can be acceptable, but unrelated world maps and awkward stock-line decorations are failures. Subject-relevant cues can be acceptable when restrained, but the prompt should not list Hong Kong, insurance, regulatory, or industry objects as keyword-triggered examples.
- `atelier-rose` is replaced by `Sovereign Gold`: premium finance, private wealth, asset management, institutional navy, antique gold, and dignified authority. The user accepts subject-relevant concrete symbols such as shields when they improve the image and serve the insurance/protection message.
- `harbor-mint` overlaps too much with `market-slate`. Reposition it as `Precision Blueprint`: technical finance, mechanisms, architecture maps, exact linework, and system explanation.
- `ink-copper` should not use a pure black stage background. Keep the dramatic executive keynote role, but change the palette to deep mineral green graphite, warm copper, ivory, muted mineral sage, and antique gold.
- `orchard-lab` keeps the distinctive clean research layout but removes plant / orchard / botanical imagery. The new direction is `Evidence Lab`: analytical dossier sheets, instrument-like linework, abstract evidence markers, and calm comparative reasoning. Template words such as `Evidence Lab`, `Evidence Rating`, `Lab Index`, and fake dossier metrics must not appear as visible page copy.

Global prompt changes:

- Core data comparison should occupy 60-70% of visual attention.
- Decorative texture and background graphics must not compress the main message.
- Donut charts, ring charts, circular nodes, and comparison circles must remain perfect circles.
- Preserve full user-provided body copy unless the user explicitly asks for compression.
- Treat raw input labels and wrappers such as `【主标题】`, `副标：`, `Subtitle:`, bullets, numbering, and parentheses as structure hints; do not render those labels or wrapper marks as visible text.
- Treat template IDs, template names, and style signatures as internal art-direction labels; do not render them as slide copy.
- Avoid unrelated literal metaphors for professional finance and board-review content, while allowing subject-relevant concrete metaphors.

## Prompt Template Card Review Optimization

The user reviewed each prompt-template issue card and approved a narrower optimization pass.

Final decisions:

- Remove template IDs, display names, and style signatures from the image-generation prompt body. Keep them only in JSON metadata, UI labels, and logs.
- Rewrite system metadata as natural language rather than labeled fields such as `页面简报`, `页面`, `版式意图`, or `Slide brief`.
- Default to full user-provided body copy in generated images unless the user explicitly asks for compression, extraction, summary, or a concise version.
- For dense content, adjust layout density and reduce decoration before deleting body copy.
- Replace the fixed `core data comparison` rule with a generic `core information body` rule; only data-comparison pages get the additional data-comparison proportion sentence.
- Remove global and conditional insurance / Hong Kong / finance metaphor examples from the base prompt. Keep one generic subject-relevance rule; do not use keyword-triggered fixed object lists.
- Delete the global hard-coded negative terms for fruit, flowers, plants, leaves, and food still life. Do not replace them with conditional injection in this pass.
- Shorten the prompt into clearer modules: hard rules, page content, visual requirements, visual style, and concise negative prompt.
- Replace abstract `visual ambition` language with executable layout requirements: one visual anchor, 3-5 ordered information groups, clear title area, and stable reading path.
- Separate `market-slate` and `harbor-mint`: the former is conclusion-oriented institutional executive briefing; the latter is mechanism / system blueprint explanation.
- Keep HEX color roles but add color proportions: roughly 70-80% background/whitespace, 15-20% structure, and no more than 5% accent.
- Replace the broad negative term `普通双栏信息图` with `廉价双栏模板感`, `库存信息图风格`, and `机械左右并列大框`.
- Do not build page-type-specific prompt templates yet; this was explicitly postponed.
- Add a fixed smoke-brief set to the testing protocol: no-copy style test, short sales page, long Chinese full-copy page, and non-financial topic page.

Validation target:

- Console prompt tests should assert that image-generation prompts no longer contain template IDs or field-label metadata, still default to full body copy, and use the simplified negative prompt.
